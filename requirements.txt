# CARDIAC-FM — minimal runtime requirements.
# Python 3.12 (env was built with 3.12.13). See env/SETUP.md for full instructions.
#
# Install torch/torchvision FIRST, matching your CUDA (see env/SETUP.md); the pins below
# are what the reference env used.

# --- core numerical / DL ---
torch==2.12.0
torchvision==0.27.0
numpy==2.4.6
scipy==1.17.1
pandas==2.3.3
scikit-learn==1.8.0          # IterativeImputer (MICE), LogisticRegression, metrics
lifelines==0.30.3            # Cox / DeepSurv C-index, Kaplan-Meier
matplotlib==3.10.9
tqdm==4.67.3
pillow
soundfile==0.14.0

# --- ECG side: ECG-FM encoder + .mat reader (NOT on PyPI; editable git @ pinned commit) ---
# Everything ECG-side (model_ecg.py, ecg_dataset.py, all downstream/cox/eval) depends on this.
# It is self-contained: it does NOT import facebookresearch/fairseq, and leaves hydra-core/omegaconf
# unpinned, so they resolve to modern versions (1.3.2 / 2.3.0) that work on Python 3.12.
#
# Do NOT install facebookresearch/fairseq: it is unused here and its pinned hydra-core 1.0.7 /
# omegaconf <2.1 conflict with Python 3.12 (the "hydra 1.0.7 / fairseq 0.12.2" error).
fairseq-signals @ git+https://github.com/Jwoo5/fairseq-signals.git@f8f0ff1c788a82c2059cb452cd5462898867489e
